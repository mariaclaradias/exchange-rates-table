import RefreshButton from './components/refresh-button/RefreshButton';

function App() {
  return (
    <div className="App">
      <RefreshButton onClick={() => {}} />
    </div>
  );
}

export default App;
